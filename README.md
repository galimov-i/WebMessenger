# Мессенджер

Легковесный мессенджер с шифрованием RSA-OAEP.

## Структура проекта

```
messenger/
├── Server/           # Серверная часть (Go)
│   ├── main.go       # Точка входа
│   ├── handlers/     # HTTP обработчики и WebSocket
│   ├── models/       # Модели данных
│   ├── crypto/       # Криптографические функции
│   └── db/           # SQLite база данных
└── Client/           # Клиентская часть (HTML/CSS/JS)
    ├── index.html    # Главная страница
    ├── css/          # Стили
    └── js/           # JavaScript модули
```

## Требования

- Go 1.21+
- SQLite (драйвер автоматически установится)

## Запуск сервера

```bash
cd Server
go mod tidy
go run main.go
```

Сервер запустится на http://localhost:8080

## Использование

1. Откройте http://localhost:8080 в браузере
2. Зарегистрируйте нового пользователя
3. Войдите в систему
4. Выберите пользователя из списка для начала общения

## Криптографическая защита

- **Алгоритм:** RSA-OAEP 2048 бит + SHA-256
- **Хэширование пароля:** bcrypt (стоимость 12)
- **Сессии:** Токены JWT-подобные, срок действия 30 дней

### Как работает шифрование

1. При регистрации генерируется пара RSA ключей
2. Открытый ключ сохраняется на сервере
3. Закрытый ключ сохраняется в браузере пользователя
4. Сообщение шифруется открытым ключом получателя
5. Расшифровка возможна только с закрытым ключом получателя

## API Endpoints

- `POST /api/register` - Регистрация
- `POST /api/login` - Вход
- `POST /api/logout` - Выход
- `GET /api/me` - Текущий пользователь
- `GET /api/users` - Список пользователей
- `GET /api/keys/:id` - Публичный ключ пользователя
- `GET /api/messages` - Сообщения
- `POST /api/messages` - Отправить сообщение
- `WS /ws` - WebSocket соединение

## Зависимости

### Сервер (Go)
- github.com/gorilla/websocket - WebSocket
- github.com/mattn/go-sqlite3 - SQLite драйвер
- golang.org/x/crypto - bcrypt

### Клиент
- Без внешних зависимостей
- Использует нативный Web Crypto API
